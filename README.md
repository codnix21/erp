# ERP System - Универсальная корпоративная ERP-система

## 📋 Описание

Универсальная ERP-система для малого и среднего бизнеса с поддержкой мультитенантности, построенная на современном стеке технологий. Система включает полный набор модулей для управления бизнес-процессами: от складского учёта до финансовой отчётности.

---

## 🏗️ Технологический стек

### Frontend
- **React 19** + **TypeScript** - UI библиотека
- **Tailwind CSS 4** - стилизация
- **Vite 7** - сборщик и dev-сервер
- **React Query** - управление состоянием сервера
- **Zustand** - клиентское состояние
- **React Router** - маршрутизация
- **React Hook Form** + **Zod** - валидация форм
- **Axios** - HTTP клиент
- **Lucide React** - иконки

### Backend
- **Node.js** + **TypeScript**
- **Fastify 5** - веб-фреймворк
- **Prisma 7** - ORM с адаптером PostgreSQL
- **PostgreSQL** - база данных
- **JWT** - авторизация (access + refresh tokens)
- **Zod** - валидация
- **Winston** - логирование
- **bcrypt 6** - хеширование паролей
- **ExcelJS** - экспорт в Excel
- **PDFKit** - генерация PDF
- **Nodemailer** - отправка email

### Инфраструктура
- **Docker** + **Docker Compose** - контейнеризация
- **Nginx** - reverse proxy (production)

---

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- PostgreSQL 15+
- npm или yarn

### Установка

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd erp
```

#### 2. Настройка Backend

```bash
cd backend
npm install

# Создайте файл .env на основе примера
cp .env.example .env

# Настройте переменные окружения:
# DATABASE_URL=postgresql://user:password@localhost:5432/erp
# JWT_SECRET=<сгенерируйте секрет минимум 32 символа>
# JWT_REFRESH_SECRET=<сгенерируйте секрет минимум 32 символа>

# Примените миграции
npx prisma migrate dev

# Заполните базу тестовыми данными
npm run prisma:seed

# Запустите сервер разработки
npm run dev
```

Backend будет доступен на `http://localhost:3000`

#### 3. Настройка Frontend

```bash
cd frontend
npm install

# Создайте файл .env (опционально)
# VITE_API_URL=http://localhost:3000

# Запустите dev-сервер
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

#### 4. Тестовые данные

После выполнения `npm run prisma:seed` будут созданы:

- **Пользователи:**
  - `admin@example.com` / `password123` (роль: Admin)
  - `manager@example.com` / `password123` (роль: Manager)
  - `accountant@example.com` / `password123` (роль: Accountant)
  - `warehouse@example.com` / `password123` (роль: Warehouse)

- **Компания:** ООО ТехноПродакшн
- **Товары, клиенты, поставщики, заказы, счета, платежи** - тестовые данные

---

## 📁 Структура проекта

```
erp/
├── backend/                 # Backend приложение
│   ├── src/
│   │   ├── config/         # Конфигурация (database, env, logger)
│   │   ├── controllers/    # Контроллеры (17 файлов)
│   │   ├── services/       # Бизнес-логика (17 файлов)
│   │   ├── middleware/     # Middleware (auth, company-scope)
│   │   ├── routes/         # API маршруты (18 файлов)
│   │   ├── utils/          # Утилиты (audit)
│   │   ├── validators/     # Zod схемы валидации (12 файлов)
│   │   ├── types/          # TypeScript типы
│   │   └── app.ts          # Точка входа
│   ├── prisma/
│   │   ├── schema.prisma   # Схема базы данных
│   │   ├── migrations/     # Миграции БД
│   │   └── seed.ts         # Заполнение тестовыми данными
│   ├── scripts/            # Вспомогательные скрипты
│   └── package.json
│
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── components/     # React компоненты (4 файла)
│   │   ├── pages/          # Страницы (38 файлов)
│   │   ├── hooks/          # Custom hooks (usePermissions)
│   │   ├── context/        # React Context (NotificationContext)
│   │   ├── store/          # Zustand store (authStore)
│   │   ├── services/       # API сервисы (13 файлов)
│   │   ├── utils/          # Утилиты (notifications, translations)
│   │   ├── types/          # TypeScript типы
│   │   ├── App.tsx         # Главный компонент
│   │   └── main.tsx        # Точка входа
│   └── package.json
│
├── docker-compose.yml      # Docker конфигурация
└── README.md
```

---

## 🔐 Система безопасности

### Аутентификация
- **JWT токены**: Access token (15 минут) + Refresh token (7 дней)
- **Хеширование паролей**: bcrypt с salt rounds = 10
- **Rate limiting**: 100 запросов в минуту
- **CORS**: Настроен с указанием origin
- **Helmet**: HTTP заголовки безопасности

### Авторизация
- **RBAC (Role-Based Access Control)**: 4 роли (Admin, Manager, Accountant, Warehouse)
- **Middleware защита**: Все API маршруты защищены `authenticate` + `requireAuth` + `requireCompany()`
- **Проверка прав**: На фронтенде (`usePermissions`) и на бэкенде (`requireRole`)
- **Мультитенантность**: Автоматическая фильтрация по `companyId`

### Защита данных
- **SQL Injection**: Защита через Prisma ORM (параметризованные запросы)
- **XSS**: Валидация всех входных данных через Zod
- **Валидация**: Zod схемы на всех эндпоинтах и формах
- **Аудит**: Логирование всех критических действий

---

## 🎭 Система ролей и прав доступа

### Роли в системе

#### 1. **Admin** (Администратор)
Полный доступ ко всем функциям системы:
- ✅ Управление пользователями (CRUD)
- ✅ Управление компаниями (CRUD)
- ✅ Товары, заказы, клиенты, поставщики (CRUD)
- ✅ Склады, счета, платежи (CRUD)
- ✅ Движения товаров, отчёты, аудит (чтение)

#### 2. **Manager** (Менеджер)
Управление продажами, клиентами и поставщиками:
- ✅ Товары (создание, чтение, обновление)
- ✅ Заказы (создание, чтение, обновление)
- ✅ Клиенты, поставщики (создание, чтение, обновление)
- ✅ Отчёты (чтение)
- ❌ НЕТ доступа к: удалению, счетам, платежам, складам, аудиту

#### 3. **Accountant** (Бухгалтер)
Финансовые операции и отчётность:
- ✅ Счета, платежи (создание, чтение, обновление)
- ✅ Отчёты (чтение)
- ✅ Заказы (только чтение)
- ❌ НЕТ доступа к: товарам, клиентам, поставщикам, складам, удалению

#### 4. **Warehouse** (Складской работник)
Управление складом и движением товаров:
- ✅ Склады (чтение, обновление)
- ✅ Товары, заказы (только чтение)
- ✅ Движения товаров (создание, чтение)
- ❌ НЕТ доступа к: созданию/удалению складов, счетам, платежам, клиентам, поставщикам

### Особенности
- **Multi-tenant**: Один пользователь может работать в нескольких компаниях
- **Множественные роли**: Пользователь может иметь несколько ролей в одной компании
- **Изоляция данных**: Каждый пользователь видит только данные своей компании
- **Кэширование ролей**: Роли кэшируются на 60 секунд для производительности

---

## 📦 Модули системы

### 1. Аутентификация и авторизация
- Регистрация/вход
- JWT + Refresh tokens
- Управление сессиями
- Восстановление пароля (готово к реализации)

### 2. Управление пользователями и ролями
- CRUD операции с пользователями
- Назначение ролей в компаниях
- Управление активностью пользователей

### 3. Компании (Multi-tenant)
- Создание/редактирование компаний
- Переключение между компаниями
- Настройки компании (валюта, налоги)

### 4. Клиенты и поставщики
- CRM функционал
- Контакты и адреса
- История взаимодействий
- Финансовая история

### 5. Товары и услуги
- Каталог товаров
- Категории (иерархические)
- Единицы измерения
- Ценообразование
- Мультивалютность
- НДС и налоги

### 6. Складской учёт
- Управление складами
- Остатки товаров
- Движение товаров (приход/расход/перемещение/корректировка)
- Автоматический пересчёт остатков
- Резервирование товаров

### 7. Заказы и договоры
- Создание заказов (покупка/продажа)
- Статусы заказов (DRAFT, PENDING, CONFIRMED, IN_PROGRESS, COMPLETED, CANCELLED)
- Связь с клиентами/поставщиками
- История изменений

### 8. Финансовые операции
- Счета (Invoices)
- Статусы счетов (DRAFT, ISSUED, PAID, PARTIALLY_PAID, OVERDUE, CANCELLED)
- Платежи
- Дебиторская/кредиторская задолженность
- НДС и налоги

### 9. Отчёты и аналитика
- Финансовые отчёты
- Отчёты по продажам
- Отчёты по складу
- Экспорт в Excel/PDF

### 10. Импорт/Экспорт
- Импорт товаров из Excel
- Экспорт заказов, товаров, счетов в Excel
- Генерация PDF для заказов и счетов
- Экспорт логов аудита

### 11. Уведомления
- Системные уведомления (success, error, warning, info)
- Централизованная обработка ошибок
- Email уведомления (готово к настройке)

### 12. Логи и аудит
- История изменений данных
- Логи действий пользователей
- Экспорт логов
- Отслеживание IP адресов

---

## 🗄️ Схема базы данных

### Основные сущности

- **users** - Пользователи системы
- **roles** - Роли (Admin, Manager, Accountant, Warehouse)
- **user_company_roles** - Связь пользователя с компанией и ролью
- **companies** - Компании (multi-tenant)
- **products** - Товары и услуги
- **categories** - Категории товаров (иерархические)
- **customers** - Клиенты
- **suppliers** - Поставщики
- **warehouses** - Склады
- **orders** - Заказы
- **order_items** - Позиции заказов
- **invoices** - Счета
- **payments** - Платежи
- **stock_movements** - Движения товаров
- **stock** - Остатки товаров на складах (материализованное представление)
- **audit_logs** - Логи аудита

Все сущности связаны с `companies` через `company_id` для изоляции данных.

---

## 🔌 API Архитектура

### RESTful принципы
- Префикс: `/api/v1/`
- Ресурсы в множественном числе
- HTTP методы: GET, POST, PUT, DELETE
- Стандартные коды ответов

### Основные эндпоинты

#### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход
- `POST /api/v1/auth/refresh` - Обновление токена
- `POST /api/v1/auth/logout` - Выход
- `GET /api/v1/auth/me` - Текущий пользователь

#### Ресурсы (все требуют аутентификации)
- `/api/v1/companies` - Компании
- `/api/v1/users` - Пользователи (только Admin)
- `/api/v1/products` - Товары
- `/api/v1/categories` - Категории
- `/api/v1/customers` - Клиенты
- `/api/v1/suppliers` - Поставщики
- `/api/v1/orders` - Заказы
- `/api/v1/invoices` - Счета
- `/api/v1/payments` - Платежи
- `/api/v1/warehouses` - Склады
- `/api/v1/stock-movements` - Движения товаров
- `/api/v1/stock` - Остатки товаров
- `/api/v1/dashboard/stats` - Статистика дашборда
- `/api/v1/reports/*` - Отчёты
- `/api/v1/export/*` - Экспорт данных
- `/api/v1/import/*` - Импорт данных
- `/api/v1/audit-logs` - Логи аудита

### Формат ответов

**Успешный ответ:**
```json
{
  "success": true,
  "data": { ... },
  "meta": { "page": 1, "limit": 20, "total": 100, "totalPages": 5 }
}
```

**Ошибка:**
```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "Описание ошибки"
  }
}
```

---

## 🛡️ Безопасность

### Реализованные меры

1. **Аутентификация**
   - JWT токены с коротким временем жизни
   - Refresh tokens для обновления сессий
   - Хеширование паролей (bcrypt, salt rounds: 10)
   - Rate limiting на эндпоинты входа

2. **Авторизация**
   - Проверка ролей на уровне middleware
   - Фильтрация по `company_id` на уровне БД
   - Валидация прав доступа к ресурсам
   - Кэширование ролей (TTL: 60 секунд)

3. **Защита данных**
   - SQL injection защита (Prisma ORM)
   - XSS защита (валидация входных данных)
   - CORS настроен с указанием origin
   - Helmet для HTTP заголовков безопасности

4. **Валидация**
   - Zod схемы на всех эндпоинтах
   - Валидация форм на фронтенде (React Hook Form + Zod)
   - Проверка типов данных

5. **Аудит**
   - Логирование всех критических действий
   - История изменений данных
   - Отслеживание IP адресов
   - Экспорт логов

---

## 📊 Функциональность

### Дашборд
- Статистика: заказы, клиенты, товары, выручка, счета, платежи, поставщики, склады
- Последние заказы
- Быстрые действия

### Управление данными
- CRUD операции для всех сущностей
- Поиск и фильтрация
- Пагинация (20 элементов на страницу)
- Сортировка

### Отчёты
- Отчёт по продажам
- Отчёт по закупкам
- Отчёт по складу
- Финансовый отчёт
- Экспорт в Excel/PDF

### Импорт/Экспорт
- Импорт товаров из Excel
- Экспорт заказов, товаров, счетов в Excel
- Генерация PDF для заказов и счетов
- Экспорт логов аудита

---

## 🚀 Развёртывание

### Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка
docker-compose down
```

### Production

1. **Backend:**
   ```bash
   cd backend
   npm run build
   npm start
   ```

2. **Frontend:**
   ```bash
   cd frontend
   npm run build
   # Разместите dist/ на веб-сервере (Nginx)
   ```

3. **Переменные окружения:**
   - `DATABASE_URL` - строка подключения к PostgreSQL
   - `JWT_SECRET` - секрет для JWT (минимум 32 символа)
   - `JWT_REFRESH_SECRET` - секрет для refresh токенов (минимум 32 символа)
   - `CORS_ORIGIN` - разрешённый origin для CORS
   - `NODE_ENV=production`

---

## 📝 Скрипты

### Backend

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка проекта
- `npm start` - Запуск production версии
- `npm run prisma:generate` - Генерация Prisma Client
- `npm run prisma:migrate` - Применение миграций
- `npm run prisma:seed` - Заполнение тестовыми данными
- `npm run prisma:studio` - Запуск Prisma Studio
- `npm run generate:secrets` - Генерация секретов для JWT

### Frontend

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Сборка для production
- `npm run preview` - Просмотр production сборки

---

## 🧪 Тестирование

### Тестовые пользователи

После выполнения `npm run prisma:seed`:

| Email | Пароль | Роль |
|-------|--------|------|
| `admin@example.com` | `password123` | Admin |
| `manager@example.com` | `password123` | Manager |
| `accountant@example.com` | `password123` | Accountant |
| `warehouse@example.com` | `password123` | Warehouse |

---

## 🔧 Конфигурация

### Backend (.env)

```env
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
DATABASE_URL=postgresql://user:password@localhost:5432/erp
JWT_SECRET=<минимум 32 символа>
JWT_REFRESH_SECRET=<минимум 32 символа>
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173
LOG_LEVEL=info

# Опционально (для email)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASSWORD=password
SMTP_FROM=noreply@example.com
```

### Frontend (.env)

```env
VITE_API_URL=http://localhost:3000
```

---

## 📈 Масштабирование

### Горизонтальное масштабирование
- Stateless backend (JWT токены)
- Load balancer для распределения нагрузки
- Репликация БД (read replicas)

### Вертикальное масштабирование
- Оптимизация запросов
- Индексы БД
- Кэширование (Redis - опционально)

### Микросервисная готовность
- Модульная архитектура
- Чёткие границы модулей
- Возможность выделения в отдельные сервисы

---

## 🐛 Отладка

### Логи
- Backend логи: `backend/logs/combined.log` и `backend/logs/error.log`
- Уровни логирования: error, warn, info, debug

### Prisma Studio
```bash
cd backend
npm run prisma:studio
```
Откроется на `http://localhost:5555`

---

## 📄 Лицензия

MIT

---

## 👥 Поддержка

Для вопросов и предложений создавайте issues в репозитории.

---

## 🎯 Roadmap

### Планируемые функции
- [ ] Восстановление пароля через email
- [ ] Push уведомления
- [ ] Мобильное приложение
- [ ] Интеграции (1C, банки)
- [ ] Расширенная аналитика
- [ ] Многоязычность
- [ ] Темная тема

---

## 📚 Дополнительная информация

### Версии библиотек

**Backend:**
- Fastify: 5.6.2
- Prisma: 7.0.0
- Zod: 4.1.12
- bcrypt: 6.0.0

**Frontend:**
- React: 19.2.0
- Vite: 7.2.4
- Tailwind CSS: 4.1.17
- React Query: 5.90.10

### Архитектурные принципы

1. **Мультитенантность**: Каждая компания изолирована на уровне БД
2. **Безопасность**: JWT + RBAC + валидация на всех уровнях
3. **Масштабируемость**: Модульная архитектура, готовность к микросервисам
4. **Производительность**: Индексы БД, пагинация, кэширование

---

**Версия:** 1.0.0  
**Последнее обновление:** 2025

