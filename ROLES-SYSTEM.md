# 🔐 Система ролей и прав доступа

## 📋 Обзор

В системе реализована **Role-Based Access Control (RBAC)** — управление доступом на основе ролей. Каждый пользователь может иметь одну или несколько ролей в рамках компании.

---

## 🎭 Роли в системе

В системе **4 предопределённые роли**:

### 1. **Admin** (Администратор)
- **Описание**: Полный доступ ко всем функциям системы
- **Права доступа**:
  - ✅ Управление пользователями (создание, чтение, обновление, удаление)
  - ✅ Управление компаниями (создание, чтение, обновление, удаление)
  - ✅ Товары (создание, чтение, обновление, удаление)
  - ✅ Заказы (создание, чтение, обновление, удаление)
  - ✅ Клиенты (создание, чтение, обновление, удаление)
  - ✅ Поставщики (создание, чтение, обновление, удаление)
  - ✅ Склады (создание, чтение, обновление, удаление)
  - ✅ Счета (создание, чтение, обновление, удаление)
  - ✅ Платежи (создание, чтение, обновление, удаление)
  - ✅ Движения товаров (создание, чтение)
  - ✅ Отчёты (чтение)
  - ✅ Аудит (чтение)

### 2. **Manager** (Менеджер)
- **Описание**: Управление продажами, клиентами и поставщиками
- **Права доступа**:
  - ✅ Товары (создание, чтение, обновление)
  - ✅ Заказы (создание, чтение, обновление)
  - ✅ Клиенты (создание, чтение, обновление)
  - ✅ Поставщики (создание, чтение, обновление)
  - ✅ Отчёты (чтение)
  - ❌ **НЕТ доступа к**: удалению товаров/заказов, счетам, платежам, складам, аудиту

### 3. **Accountant** (Бухгалтер)
- **Описание**: Финансовые операции и отчётность
- **Права доступа**:
  - ✅ Счета (создание, чтение, обновление)
  - ✅ Платежи (создание, чтение, обновление)
  - ✅ Отчёты (чтение)
  - ✅ Заказы (только чтение)
  - ❌ **НЕТ доступа к**: товарам, клиентам, поставщикам, складам, удалению

### 4. **Warehouse** (Складской работник)
- **Описание**: Управление складом и движением товаров
- **Права доступа**:
  - ✅ Склады (чтение, обновление)
  - ✅ Товары (только чтение)
  - ✅ Заказы (только чтение)
  - ✅ Движения товаров (создание, чтение)
  - ❌ **НЕТ доступа к**: созданию/удалению складов, счетам, платежам, клиентам, поставщикам

---

## 🏗️ Архитектура системы ролей

### Структура базы данных

```
User (Пользователь)
  ↓
UserCompanyRole (Связь пользователя с компанией и ролью)
  ↓
Company (Компания) + Role (Роль)
```

### Таблицы в базе данных

1. **`users`** — пользователи системы
   - `id`, `email`, `passwordHash`, `firstName`, `lastName`, `isActive`

2. **`roles`** — роли системы
   - `id`, `name` (Admin, Manager, Accountant, Warehouse)
   - `permissions` (JSON с разрешениями)
   - `description`

3. **`companies`** — компании (multi-tenant)
   - `id`, `name`, `inn`, `address`, `phone`, `email`

4. **`user_company_roles`** — связь пользователя с компанией и ролью
   - `userId` → `users.id`
   - `companyId` → `companies.id`
   - `roleId` → `roles.id`
   - `isActive` — активна ли связь

### Важные особенности

- ✅ **Multi-tenant**: Один пользователь может работать в нескольких компаниях
- ✅ **Множественные роли**: Пользователь может иметь несколько ролей в одной компании
- ✅ **Изоляция данных**: Каждый пользователь видит только данные своей компании




## 📊 Примеры пользователей

### Тестовые пользователи (из seed.ts)

| Email | Роль | Что видит в системе |
|-------|------|---------------------|
| `admin@example.com` | Admin | **Всё**: товары, заказы, клиенты, поставщики, склады, счета, платежи, отчёты, аудит |
| `manager@example.com` | Manager | **Продажи**: товары, заказы, клиенты, поставщики, отчёты (без удаления, без счетов/платежей) |
| `accountant@example.com` | Accountant | **Финансы**: счета, платежи, отчёты, просмотр заказов (без редактирования) |

**Пароль для всех**: `password123`

### 3. Применить миграцию

```bash
cd backend
npx prisma migrate dev --name add_new_role
npm run seed
```

---

## ⚠️ Важные моменты

1. **Admin имеет все права**: Если у пользователя есть роль `Admin`, он автоматически получает доступ ко всем функциям, независимо от других ролей.

2. **Роли кэшируются**: Роли пользователя кэшируются на 60 секунд в памяти для улучшения производительности.

3. **Multi-tenant**: Каждый пользователь работает в контексте своей компании. Данные изолированы по `companyId`.

4. **Активные роли**: Только роли с `isActive: true` учитываются при проверке прав.

5. **Связь через UserCompanyRole**: Пользователь не может иметь роль напрямую. Роль всегда привязана к пользователю **в рамках конкретной компании**.

---

## 📝 Резюме

- **4 роли**: Admin, Manager, Accountant, Warehouse
- **Каждая роль** имеет свой набор разрешений
- **Пользователь** может иметь несколько ролей в одной компании
- **Права проверяются** на фронтенде (UI) и на бэкенде (API)
- **Admin** имеет полный доступ ко всему

---

## 🔗 Связанные файлы

- `backend/prisma/schema.prisma` — схема базы данных
- `backend/prisma/seed.ts` — создание тестовых ролей и пользователей
- `backend/src/services/auth.service.ts` — логика логина и получения ролей
- `backend/src/middleware/auth.ts` — проверка прав на бэкенде
- `frontend/src/hooks/usePermissions.ts` — проверка прав на фронтенде
- `frontend/src/components/ProtectedRoute.tsx` — защита маршрутов
- `frontend/src/components/Layout.tsx` — условное отображение меню

